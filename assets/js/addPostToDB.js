addPostToDB();

function addPostToDB(user_email){
	

//	ID int NOT NULL AUTO_INCREMENT,
//	type varchar(20) NOT NULL,
//	user_id int NOT NULL, --
//	date datetime NOT NULL DEFAULT CURRENT_TIMESTAMP, ---
//	location varchar(20) NOT NULL, ---
//	category varchar(20) NOT NULL, ---
//	occupation varchar(20) NOT NULL, ---
//	body_description varchar(255) NOT NULL, ---
//	due_date varchar(20), 
//	contact varchar(20) NOT NULL,  
	
	
	var div_occuptationOffering = document.getElementById('createpostoccupationoffering');
	var div_descriptionOffering = document.getElementById('createpostdescriptionoffering');
	var div_category = document.getElementById('createpostcategories');
	var div_location = document.getElementById('location');
	
	
	
}